# gitar-demo

Main Application: Backend(flask API) that stores items with titles and tags, and lets you search and remove them

PR Request: when someone adds tags to an item, we want to make sure duplicate tags don't sneak in. Example: you tag something "urgent" twice, it should only show up once. A small, reasonable usability fix.

